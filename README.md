# BaseLogger: минималистичная и эффективная библиотека для логирования на Go

Библиотека для структурированного логирования, написанная на чистом Go. Разработана как учебный проект для глубокого понимания принципов создания повторно используемых пакетов, работы с уровнями логирования и форматированием вывода.

Проект демонстрирует осознанный подход к разработке библиотек: фокус на **чистом API**, **производительности** (минимальные накладные расходы) и **простоту интеграции**.

---

## ✨ Особенности

*   **Простой и интуитивный API**: Предоставляет функции `Debug()`, `Info()`, `Warning()`, `Error()`, `Fatal()` для каждого уровня.
*   **Фильтрация по уровням**: Глобальная настройка минимального уровня логирования через `SetMinLevel()`. Сообщения более низких уровней игнорируются, что повышает производительность в production-среде.
*   **Структурированный вывод**: Каждое сообщение автоматически включает метку времени в формате RFC3339 (UTC), уровень логирования и само сообщение.
*   **Поддержка форматирования**: Все методы принимают строку формата и аргументы аналогично `fmt.Printf`.
*   **Лёгковесность**: Нет внешних зависимостей, используется только стандартная библиотека Go.
*   **Детерминированное поведение**: Уровни логирования имеют строгий порядок (DEBUG < INFO < WARNING < ERROR < FATAL).

---

## 🚀 Быстрый старт

### Установка

```bash
go get github.com/Rkmfm2523/BaseLogger

package main

import (
    "github.com/Rkmfm2523/BaseLogger/logger" // Или ваш локальный путь
)

func main() {
    // Устанавливаем уровень: будут выводиться только логи INFO и выше
    logger.SetMinLevel(logger.INFO)

    logger.Debug("Это сообщение не будет выведено") // Уровень DEBUG < INFO
    logger.Info("Запуск приложения")
    logger.Warning("Конфигурационный файл не найден, используются значения по умолчанию")
    logger.Error("Не удалось подключиться к базе данных: %v", connectionErr)

    // Логи с форматированием
    userID := 42
    logger.Info("Пользователь с ID=%d выполнил вход", userID)
}

** Пример:
2024-12-11T10:30:00Z [INFO] Запуск приложения
2024-12-11T10:30:01Z [WARNING] Конфигурационный файл не найден, используются значения по умолчанию
2024-12-11T10:30:02Z [ERROR] Не удалось подключиться к базе данных: dial tcp 127.0.0.1:5432: connect: connection refused
2024-12-11T10:30:03Z [INFO] Пользователь с ID=42 выполнил вход
